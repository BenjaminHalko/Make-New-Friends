/// @desc 

EnableLive;

properties = variable_clone(global.currentShape,1);
radius = oShapeSummoner.shapeRadius * 56;
angle = oShapeSummoner.shapeAngle;
faceAlpha = 1;
faceAngle = oShapeSummoner.faceAngle;